{
  "name": "tali-core-identity-billing",
  "version": "0.1.0",
  "private": true,
  "description": "Tali Core - first slice (Identity & Profile + Billing & Entitlements), strangler-pattern migration off n8n/Airtable per Этап 7 аудита.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "psql \"$DATABASE_URL\" -f schema.sql",
    "verify-hmac": "node scripts/verify-hmac-vector.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
